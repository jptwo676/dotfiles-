set number      "shows numbers

set hidden      "don't question about existing buffers 

set splitright  "change files positions when they are created
set splitbelow  "change files positions when they are created

